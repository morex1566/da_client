using UnityEngine;

public enum RangeWeaponFireModeType
{
    Auto,
    SemiAuto,
    Burst
}

public enum RangeWeaponStateType
{
    Idle,
    Reload
}

public enum RangeWeaponStateFlagsType
{

}

public enum WeaponAttachmentType
{
    Mag,
    Grip,
    Assembly,
    UpperBody,
    Scope,
    Muzzle
}

public enum MeleeWeaponStateType
{
    LeftReady,
    RightReady,
    Swing
}